version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: certificate-backend
    ports:
      - "8000:8000"  # Expose for local development (frontend connects directly)
    expose:
      - "8000"  # Also expose internally for nginx
    environment:
      - DEBUG=False
      # SMTP Configuration (for sending to students)
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-465}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_USE_SSL=${SMTP_USE_SSL:-true}
      - SMTP_USE_TLS=${SMTP_USE_TLS:-false}
      - EMAIL_FROM=${EMAIL_FROM}
      # Admin receiver for ZIP results
      - ADMIN_RESULTS_EMAIL=${ADMIN_RESULTS_EMAIL}
      # General settings
      - TEMPLATE_PATH=${TEMPLATE_PATH:-/app/data/template.png}
      - FIELDS_CONFIG_PATH=${FIELDS_CONFIG_PATH:-/app/fields_config.json}
      - FONTS_DIR=${FONTS_DIR:-/app/fonts}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:5500,http://localhost:5501,http://127.0.0.1:5500,http://127.0.0.1:5501,https://jabanes.github.io}
      # Frontend URL (for reference only, not served by backend)
      - FRONTEND_URL=${FRONTEND_URL:-}
    volumes:
      - ./backend/data:/app/data
      - ./backend/logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  nginx:
    image: nginx:alpine
    container_name: certificate-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
    restart: unless-stopped

